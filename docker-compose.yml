version: "3.8"

services:
  app:
    container_name: matchup
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    networks:
      - web-network

networks:
  web-network:
    external: true